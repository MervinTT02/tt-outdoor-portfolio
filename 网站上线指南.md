# TT 个人户外摄影网站上线指南

## 1. 先在本地预览网站
在项目根目录运行：

```bash
python3 -m http.server 8080
```

浏览器打开：

`http://localhost:8080`

如果页面和图片都正常显示，就可以发布。

## 2. 申请个人域名（推荐）
推荐域名示例：

- `ttoutdoors.com`
- `tthikingphoto.com`
- `tttrails.cn`

可在以下平台申请：

- 阿里云万网
- 腾讯云 DNSPod
- Cloudflare Registrar
- Namecheap

建议：优先 `.com`，其次 `.cn` 或 `.photo`。

## 3. 免费托管发布（推荐 Cloudflare Pages）
1. 把该项目推送到 GitHub 仓库。
2. 登录 Cloudflare，进入 `Pages`，选择 `Connect to Git`。
3. 选择该仓库。
4. 构建设置：
   - Framework preset: `None`
   - Build command: 留空
   - Build output directory: `/`（根目录）
5. 点击部署。

部署后会得到一个 `*.pages.dev` 临时域名。

## 4. 绑定你申请的域名
1. 在 Cloudflare Pages 项目里点击 `Custom domains`。
2. 输入你的域名，例如 `ttoutdoors.com`。
3. 按提示添加 DNS 记录（一般是 `CNAME`）。
4. 等待几分钟到几十分钟，HTTPS 会自动签发。

## 5. 后续更新方式
每次你新增照片或改页面后：

1. 提交并推送到 GitHub。
2. Cloudflare Pages 自动重新部署。
3. 线上网站自动更新。

## 6. 可选增强（下一步）
- 增加 GPX 轨迹地图（Mapbox/Leaflet）
- 增加路线详情页（每条路线单独页面）
- 增加中英文切换
- 增加图片压缩与 WebP 自动化脚本（提高打开速度）
